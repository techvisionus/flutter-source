import 'package:flutter/material.dart';

final ASV_HintTextStyle1 =
    TextStyle(color: Colors.grey, fontFamily: 'OpenSans', fontSize: 21.0);

final ASV_LabelStyle1 = TextStyle(
  color: Colors.black,
  fontWeight: FontWeight.bold,
  fontFamily: 'OpenSans',
);

final ASV_BoxStyle1 = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.circular(5.0),
  boxShadow: [
    BoxShadow(
      color: Colors.black12,
      blurRadius: 6.0,
      offset: Offset(0, 2),
    ),
  ],
);
